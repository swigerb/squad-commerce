# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: squad-commerce
services:  
  app:
    language: dotnet
    project: ./src/SquadCommerce.AppHost/SquadCommerce.AppHost.csproj
    host: containerapp
